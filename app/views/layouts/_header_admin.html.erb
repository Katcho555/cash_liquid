<nav class="navbar navbar-expand-lg navbar-dark bg-dark navbar-desktop shadow-sm">
  <div class="container">

    <!-- Logo / Brand -->
    <a class="navbar-brand fw-bold d-flex align-items-center" href="<%= dashboard_index_path %>">
      <i class="bi bi-graph-up-arrow text-warning me-2"></i>
      <span>Cash<span class="text-warning">Liquid</span></span>
    </a>

    <!-- Toggle mobile -->
    <button class="navbar-toggler border-0" type="button"
            data-bs-toggle="collapse"
            data-bs-target="#navbarNav">
      <i class="bi bi-list text-white fs-3"></i>
    </button>

    <!-- Menu -->
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-auto align-items-lg-center gap-lg-2">

        <li class="nav-item">
          <a class="nav-link <%= 'active' if current_page?(dashboard_index_path) %>"
             href="<%= dashboard_index_path %>">
            <i class="bi bi-house-door me-1"></i>
            Accueil
          </a>
        </li>

        <li class="nav-item">
          <a class="nav-link <%= 'active' if request.path.start_with?(products_path) %>"
             href="<%= products_path %>">
            <i class="bi bi-box-seam me-1"></i>
            Produits
          </a>
        </li>

        <li class="nav-item">
          <a class="nav-link <%= 'active' if current_page?(dashboard_list_utilisateur_path) %>"
             href="<%= dashboard_list_utilisateur_path %>">
            <i class="bi bi-people me-1"></i>
            Équipes
            <span class="badge bg-warning text-dark ms-1">
              <%= current_user.filleuls.count %>
            </span>
          </a>
        </li>

        <!-- Separator -->
        <li class="nav-item d-none d-lg-block">
          <span class="nav-link text-muted">|</span>
        </li>

        <!-- Profile -->
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle d-flex align-items-center"
             href="#"
             role="button"
             data-bs-toggle="dropdown">
            <i class="bi bi-person-circle fs-5 me-1"></i>
            <%= current_user.prenom %>
          </a>

          <ul class="dropdown-menu dropdown-menu-end shadow-sm">
            <li>
              <a class="dropdown-item" href="<%= user_profile_path %>">
                <i class="bi bi-person me-2"></i> Mon compte
              </a>
            </li>
            <li>
              <a class="dropdown-item text-danger" href="<%= logout_path %>" data-turbo-method="delete">
                <i class="bi bi-box-arrow-right me-2"></i> Déconnexion
              </a>
            </li>
          </ul>
        </li>

      </ul>
    </div>
  </div>
</nav>


<div class="market-banner bg-warning bg-opacity-10 py-2 border-bottom">
  <div class="container text-center fw-semibold text-dark">
    <i class="bi bi-fuel-pump text-warning me-2"></i>
    Marché pétrolier · Opportunités d’investissement actives
  </div>
</div>
